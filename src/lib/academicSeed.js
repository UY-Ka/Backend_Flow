import Faculty from "../models/Faculty.js";
import Program from "../models/Program.js";
import Group from "../models/Group.js";
import ScheduleItem from "../models/ScheduleItem.js";

/**
 * Эта функция была удалена, так как она добавляла демо-данные.
 * Вместо этого, данные для студентов должны вводиться администратором.
 * @deprecated
 */
export async function ensureDefaultAcademicData() {
  console.log("Функция ensureDefaultAcademicData больше не используется. Данные должны вводиться вручную.");
  // Пустая функция, не создает демо-данные.
  return {};
}

